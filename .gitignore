flatpaks_with_deps
flatpak.*
cosign.key

*_build
*_build.*
previous.manifest.json
changelog.md
output.env
version.txt

devcontainer

# Python cache
__pycache__/
*.py[cod]
*$py.class

# macOS
.DS_Store

# Claude Code local state (keep shared config like settings.json, commands/, agents/, hooks/)
.claude/settings.local.json
.claude/scheduled_tasks.lock
